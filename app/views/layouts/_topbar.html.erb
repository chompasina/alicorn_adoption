<nav class="top-nav navbar navbar-default navbar-fixed-top">
  <div class="row">
    
    <div class="col-md-9 navbar-header">
    </div>
      
    <div class="col-md-3">
      <ul class="top-nav-list">
        <li id="cartCounter"><span class="glyphicon glyphicon-shopping-cart" aria-hidden="true"></span><%= link_to "Cart: #{@cart.total}", cart_path %></li>
        <% if current_user %>
          <li><%= "Logged in as #{current_user.username}" %> | <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span><%= link_to "Logout", logout_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Login |", login_path %> <%= link_to "Register", new_user_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav> 